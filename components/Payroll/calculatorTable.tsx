interface TabPanelProps {
    children?: React.ReactNode;
    index: any;
    value: any;
};

const calculatorTable = (props: TabPanelProps) => {

    return(
        <div>
        </div>
    )

 }

 export default calculatorTable