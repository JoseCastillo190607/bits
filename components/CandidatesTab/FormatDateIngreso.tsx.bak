import moment from "moment";

const FormatDate = ({ FechaIngreso }: any) => {
    return (
        <span>
            {moment(FechaIngreso).format('DD/MM/YYYY')}
        </span>
    )
}

export default FormatDate;