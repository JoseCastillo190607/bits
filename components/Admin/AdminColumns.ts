import { TBColumn } from '../../interfaces/TableBits.interfaces';
const columns: TBColumn[] = [
    {
        id: "FullName",
        label: 'Administrador',
        align: 'left',
    },
];

export default columns;